name: 'guest_flow_v2'
version: '1.0.0'
config-version: 2

profile: 'guest_flow_v2'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  guest_flow_v2:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
